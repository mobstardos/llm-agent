id: security_scan_secrets
title: Security — поиск секретов
category: dev
tags: [smoke, dev, security]

fixture:
  type: temp_project
  params:
    files:
      "config.py": |
        AWS_KEY = "AKIAIOSFODNN7EXAMPLE"
        API_KEY = "sk-1234567890abcdefghijklmnopqrstuv"

prompt: "Проверь проект на утечки секретов"

options:
  agent_id: security
  mcp_servers: [security, filesystem]

assertions:
  - type: no_errors
    severity: soft
    params: {}

loop: agent.reasoning
