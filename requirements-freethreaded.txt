# ═══════════════════════════════════════════════════════════════════════
# LLM Agent — зависимости для FREE-THREADED сборок Python (3.13t/3.14t)
# Выбирается install.bat/install.sh АВТОМАТИЧЕСКИ при обнаружении t-сборки.
# Отличия от requirements.txt (нет t-колёс на PyPI, win_amd64, 2026-09):
#   lancedb, sentence-transformers (tokenizers/safetensors abi3),
#   tree-sitter + грамматики (abi3), duckdb, aiokafka (только cp314),
#   faster-whisper (транзитивные onnxruntime/tokenizers без t-колёс);
#   psycopg — чистый (pure-python), без libpq-бинарника.
#   Деградация: векторное хранилище -> postgres/файлы, embedder -> hashing,
#   граф кода/MCP data/CDC/whisper -> отключены, PG-фичи -> 503,
#   пока libpq не появится в PATH (сам сервер стартует нормально).
#   Проверено dry-run резолвером pip под cp314t/win_amd64: граф сходится.
# ═══════════════════════════════════════════════════════════════════════
# ─── Web-сервер ─────────────────────────────────────────────────────────
fastapi>=0.115.0
# uvicorn БЕЗ [standard]: uvloop не собирается под t; watchfiles ставим явно
uvicorn>=0.30.6
watchfiles>=1.0.0
websockets>=13.1

# ─── LLM и HTTP ─────────────────────────────────────────────────────────
openai>=1.51.0
httpx>=0.27.2
httpx[http2]>=0.27.2
aiohttp>=3.10.0

# ─── MCP ────────────────────────────────────────────────────────────────
# ВАЖНО: mcp 2.x удалил декораторы @app.list_tools() — проект собран под 1.x
mcp>=1.1.3,<2.0

# ─── Валидация и конфиги ────────────────────────────────────────────────
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
PyYAML>=6.0.2

# ─── Базы данных ────────────────────────────────────────────────────────
# MySQL / SQLite
pymysql>=1.1.1
# PostgreSQL — основной драйвер (psycopg3). Альтернативы:
#   psycopg2-binary>=2.9.9  (старее, но стабильнее на части Windows)
#   pg8000>=1.31.5          (pure-python fallback)
# run.py сам определяет, какой драйвер доступен.
# БЕЗ [binary]: psycopg-binary не имеет t-сборок. Чистый psycopg ставим
# ЯВНО: psycopg_pool НЕ объявляет psycopg зависимостью, а src/db/pool.py
# импортирует psycopg на верхнем уровне — без него сервер не стартует.
# Чистый psycopg импортируется без libpq; libpq нужен только при
# подключении — без него PG-фичи деградируют в 503 (см. install.*).
psycopg>=3.2.0
psycopg_pool>=3.2.0
pgvector>=0.3.0

# ─── Память проекта ─────────────────────────────────────────────────────
# LanceDB пропущена: нет t-колёс (векторное хранилище — postgres/файлы).
# pyarrow остаётся (есть cp314t; нужен pandas 3.x).
pyarrow>=17.0.0
# sentence-transformers пропущена (tokenizers/safetensors — abi3):
# embedder auto -> hashing (src/memory/embedders.py деградирует сам).
# Токенизация (бюджет контекста)
tiktoken>=0.7.0
# Хэширование (incremental index)
xxhash>=3.4.0
# SQL парсинг (граф знаний)
sqlparse>=0.5.0

# ─── Ollama (малая LLM для фонового обогащения памяти) ─────────────────
ollama>=0.4.0
# Ollama устанавливается отдельно: https://ollama.com/download

# ─── File watching / мониторинг ─────────────────────────────────────────
watchdog>=4.0.0
psutil>=6.0.0

# ─── Extraction (PDF, DOCX, XLSX, изображения) ──────────────────────────
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
Pillow>=10.4.0
numpy>=2.0.0
scikit-image>=0.24.0
pytesseract>=0.3.13

# Tree-sitter (+10 грамматик) пропущен: только cp314/abi3-колёса.
# Граф кода деградирует (src/memory/graph_parsers импортируют лениво).
# ─── Browser MCP (Playwright) ───────────────────────────────────────────
playwright>=1.48.0
# Установка браузеров: playwright install chromium

# ─── Media MCP (аудио/видео) ────────────────────────────────────────────
# faster-whisper ПРОПУЩЕН: тянет onnxruntime и tokenizers — у обоих НЕТ
# t-колёс (win_amd64, abi3), pip валил бы установку ВСЕГО файла целиком
# (проверено dry-run резолвером: ResolutionImpossible). Транскрипция
# речи недоступна (src/media/whisper_runner.py деградирует сам);
# ffmpeg-функции остаются — ffmpeg-python pure-python.
ffmpeg-python>=0.2.0

# ─── Cluster / Multi-instance (опционально) ─────────────────────────────
redis>=5.0.0

# aiokafka пропущен (только cp314): CDC-источник Kafka недоступен.
# ─── Dev tools (P0) ─────────────────────────────────────────────────────
# ripgrep и fd устанавливаются как бинари, не через pip
pathspec>=0.12.0
# Встроенный PostgreSQL для тестов БД (scripts/test_db_pg.py, scripts/e2e_db_pg.py)
pgserver>=0.1.6; platform_system != "Windows"

# ─── Dev tools (P1) ─────────────────────────────────────────────────────
pip-audit>=2.7.0
bandit>=1.7.0
safety>=3.2.0
PyGithub>=2.4.0
alembic>=1.13.0
pytest>=8.3.0
pytest-cov>=5.0.0
pytest-asyncio>=0.24.0
respx>=0.21.0
mutmut>=2.5.0
py-spy>=0.3.14
# memray пропущен: под t-сборки колёс нет, sdist с C-расширением не собирается.

# ─── Dev tools (P2) ─────────────────────────────────────────────────────
pymongo>=4.9.0
elasticsearch>=8.15.0
pymssql>=2.3.0; platform_system != "Darwin"
griffe>=1.3.0
# pydoc-markdown пропущен: пин docstring-parser<0.12 существует только в
# sdist (без колёс) — на t-сборках рвёт резолв (проверено dry-run pip).
# Генерация доков — dev-инструмент, рантайму не нужен.
# duckdb пропущен (только cp314): MCP data-инструменты SQL-аналитики деградируют.
pandas>=2.2.0
cssutils>=2.11.0

# ─── Kubernetes (опционально) ───────────────────────────────────────────
# kubectl — установить отдельно: https://kubernetes.io/docs/tasks/tools/
kubernetes>=31.0.0
kubernetes-asyncio>=31.0.0

# ─── CLI ────────────────────────────────────────────────────────────────
rich>=13.9.0
