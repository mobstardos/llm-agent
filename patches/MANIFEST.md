# Патчи из чата, не слитые автоматически

Эти фрагменты помечены в чате как «добавления/заменить» и требуют ручного слияния
в соответствующие файлы (или не требуются, если итоговая полная версия уже содержит их).

- `config/settings.yaml` (MSG 99, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
- `config/settings.yaml` (MSG 101, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
- `config/settings.yaml` (MSG 105, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
- `config/settings.yaml` (MSG 107, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
- `config/settings.yaml` (MSG 109, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
- `config/settings.yaml` (MSG 111, — **ЗАМЕНИТЬ** секцию `mcp_servers`)
